When changeLastCharToUpper() method is not completed : it returns null, and which is not expected output, so our unit testing
failed.

When  changeLastCharToUpper() method is implemented : it returns result, and out unit testing get succeed.